# Admin backend API base URL (used by axios in src/api/client.ts).
# Defaults to http://localhost:8000 if unset.
VITE_API_URL=http://localhost:8000

# Set this when serving the admin app from a subpath, for example /admin/.
VITE_ADMIN_BASE_PATH=/
